language: 'ruby'
sudo: false
rvm:
  - '2.3.0'

jdk:
  - 'oraclejdk8'

install:
  - 'bundle install'
  - 'pip install --user html5validator'

script:
  - 'bundle exec rake test'
  - 'bundle exec rake build'
  - 'html5validator --root _site/ --ignore 'Attribute "⚡" not allowed' 'Attribute "amp-boilerplate" not allowed' 'Attribute "custom-element" not allowed''
